# Q22 — OOP: __str__ and __repr__
# Take your Dog class from Q21.
# Add:
#   - __str__  → returns "Dog(name=Rex)"         (human-readable, used by print())
#   - __repr__ → returns "Dog('Rex', 'Labrador')" (developer-readable, used in console)
#
# Test: print(dog) should use __str__, repr(dog) should use __repr__
