# Q24 — OOP: Inheritance
# Create a base class Animal with:
#   - __init__(self, name)
#   - speak() → prints "Some animal sound"
#
# Create two subclasses: Dog and Cat
#   - Dog.speak() → prints "Woof! I am {name}"
#   - Cat.speak() → prints "Meow! I am {name}"
#
# Create a list with one Dog and one Cat, loop through and call speak() on each.
# This is polymorphism — same method name, different behaviour.
