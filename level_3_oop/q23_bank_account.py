# Q23 — OOP: BankAccount Class
# Create a BankAccount class with:
#   - __init__(self, owner, balance=0)
#   - deposit(amount)   → adds to balance, prints new balance
#   - withdraw(amount)  → subtracts from balance, but raise ValueError if insufficient funds
#   - a balance property that returns the current balance (make _balance private)
#
# Test it: deposit 500, withdraw 200, try withdrawing 400 (should raise error)
