# Q36 — Power Features: *args and **kwargs
# Write these three functions:
#
# 1. total(*args) → accepts any number of numbers, returns their sum
#    total(1, 2, 3, 4) → 10
#
# 2. display(**kwargs) → prints each key-value pair on a new line
#    display(name="Alice", age=30) → prints "name: Alice" and "age: 30"
#
# 3. mixed(required, *args, **kwargs) → accepts one required arg, then any extras
#    Print all three: required, args tuple, kwargs dict
#
# *args  → collects extra positional arguments as a tuple
# **kwargs → collects extra keyword arguments as a dict
