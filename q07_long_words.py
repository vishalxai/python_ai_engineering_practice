# Q7 — Level 2: Collections
# Given this list of words:
words = ["hi", "hello", "cat", "elephant", "dog", "python", "ai"]
# Print only the words that are longer than 4 characters.

for word in words :
    if len(word)>4:
        print(word)